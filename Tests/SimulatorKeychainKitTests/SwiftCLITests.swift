@testable import SimulatorKeychainKit
import XCTest

final class SimulatorKeychainKitTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SimulatorKeychainKit().text, "Hello, World!")
    }
}
